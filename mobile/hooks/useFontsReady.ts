/**
 * Font bootstrap for splash gating.
 * Custom Manrope assets can be loaded here before returning true.
 */
export function useFontsReady(): boolean {
  // System fonts only today — no async load required.
  return true;
}
