name: Preview build on main

# Push to main with changes under mobile/** triggers an Android preview build.

on:
  push:
    branches:
      - main
    paths:
      - mobile/**

jobs:
  build_android_preview:
    name: Android preview
    type: build
    params:
      platform: android
      profile: preview
