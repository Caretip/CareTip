name: Preview build on main

on:
  push:
    branches:
      - main
    paths:
      - mobile/**

jobs:
  build_android_preview:
    name: Android preview
    type: build
    params:
      platform: android
      profile: preview
